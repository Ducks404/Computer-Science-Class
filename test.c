#include <stdio.h>
// #include <string.h>

int main(void)
{
    // int num = 7;
    // int *p = &num;
    // printf("num is %d\n", num);
    // printf("num is stored in %x\n", &num);
    // printf("p points to %x\n", p);
    char operator;
    scanf("%c", &operator);
    int op = atoi(operator);
    printf("%d", op);
    return 0;
}